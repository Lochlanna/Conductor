[package]
name = "conductor_derive"
version = "0.1.0"
edition = "2018"

[lib]
proc-macro = true

[dependencies]
syn = {version = "1.0", features = ["extra-traits"]}
conductor_shared = { path = "../conductor_shared" }
quote = "1.0"

proc-macro2 = "1.0"